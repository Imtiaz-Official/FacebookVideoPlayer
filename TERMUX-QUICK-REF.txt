╔═══════════════════════════════════════════════════════════════╗
║                                                               ║
║     📱 TERMUX QUICK REFERENCE - FACEBOOK VIDEO PLAYER        ║
║                                                               ║
╚═══════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────┐
│ 🚀 QUICK START (ONE COMMAND)                                │
└─────────────────────────────────────────────────────────────┘

  bash run.sh

  That's it! Everything else is automatic.


┌─────────────────────────────────────────────────────────────┐
│ 📋 MANUAL COMMANDS (if needed)                              │
└─────────────────────────────────────────────────────────────┘

  # Setup from scratch
  bash setup-termux.sh

  # Run both servers
  npm run dev:all

  # Run frontend only
  npm run dev

  # Run backend only
  npm run server


┌─────────────────────────────────────────────────────────────┐
│ 🌐 ACCESS URLs                                              │
└─────────────────────────────────────────────────────────────┘

  On Phone:
  → http://localhost:5173

  From Other Devices (same WiFi):
  → http://YOUR_PHONE_IP:5173
  
  Get IP: ifconfig | grep inet


┌─────────────────────────────────────────────────────────────┐
│ 🔧 TROUBLESHOOTING                                          │
└─────────────────────────────────────────────────────────────┘

  Port busy:
  → pkill node

  Can't access from other devices:
  → Check WiFi - must be same network
  → Disable firewall on accessing device

  Storage permission error:
  → termux-setup-storage

  Dependencies fail:
  → npm install --legacy-peer-deps


┌─────────────────────────────────────────────────────────────┐
│ 💾 KEEP RUNNING IN BACKGROUND                               │
└─────────────────────────────────────────────────────────────┘

  pkg install tmux        # Install tmux
  tmux                    # Start session
  bash run.sh             # Run app
  Ctrl+B then D          # Detach (keeps running)
  tmux attach            # Reattach later


┌─────────────────────────────────────────────────────────────┐
│ ⚡ FIRST TIME SETUP (Do Once)                               │
└─────────────────────────────────────────────────────────────┘

  1. pkg update && pkg upgrade
  2. pkg install nodejs-lts
  3. pkg install chromium (optional)
  4. bash run.sh


┌─────────────────────────────────────────────────────────────┐
│ 📁 FILE LOCATIONS                                           │
└─────────────────────────────────────────────────────────────┘

  Termux Home:     ~/
  Internal Storage: /sdcard/
  Downloads:        /sdcard/Download/
  
  Navigate:
  → cd /sdcard/Download/facebook-video-player


┌─────────────────────────────────────────────────────────────┐
│ ⚙️ USEFUL TERMUX COMMANDS                                   │
└─────────────────────────────────────────────────────────────┘

  pkg search <name>      # Search packages
  pkg install <name>     # Install package
  pkg list-installed     # Show installed
  termux-info           # System info
  termux-wake-lock      # Prevent sleep
  termux-wake-unlock    # Allow sleep


┌─────────────────────────────────────────────────────────────┐
│ 🔥 TIPS                                                     │
└─────────────────────────────────────────────────────────────┘

  • Disable battery optimization for Termux
  • Use tmux to keep app running
  • First install takes 5-10 minutes
  • WiFi must stay on for network access
  • Clear cache if UI doesn't update (Ctrl+Shift+R)


╔═══════════════════════════════════════════════════════════════╗
║  Need help? Check TERMUX.md for detailed instructions       ║
╚═══════════════════════════════════════════════════════════════╝
